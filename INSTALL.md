# Urbyte Knowledge Base

The `docs` and `seo` directories contain Urbyte's public documentation and structured-data assets.

These files do not alter the live website unless separately integrated into the application.
